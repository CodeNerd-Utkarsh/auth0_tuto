import React from 'react'
import { useAuth0 } from '@auth0/auth0-react'
const Logout = () => {

    return (
        <button
        // onClick={(e)=>}
        >Logout</button>
    )
}

export default Logout